name: Subida a Desarrollo

on:
  push:
    branches: [ "desarrollo" ]

jobs:

  # --------------------- COMPILAR, TESTEAR Y CREAR IMAGEN ---------------------
  construir_imagen_snapshot:
    runs-on: self-hosted
    env:
      VERSION: snapshot
    steps:
      # Descargar el código
      - uses: actions/checkout@v4

      # Configurar Java 17 sin cache remota (usamos ~/.m2 del runner)
      - name: Configurar JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

  compilar_y_testear:
    needs: construir_imagen_snapshot
    runs-on: self-hosted
    env:
      VERSION: snapshot
    steps:
      - name: Restaurar caché de compilación (target)
        uses: actions/cache@v4
        with:
          path: target
          key: target-cache-${{ runner.os }}-${{ hashFiles('**/pom.xml', '**/*.java') }}
          restore-keys: |
            target-cache-${{ runner.os }}-
            
      # Compilar, testear y usar dependencias desde ~/.m2 local (más rápido en self-hosted)
      - name: Compilar y ejecutar tests
        run: mvn -T32 --batch-mode verify

      # Construir imagen Docker usando el builder ligero "minimal" (más rápido)
      - name: Construir imagen Docker con builder ligero
        run: mvn -T32 spring-boot:build-image -DskipTests

      - name: Guardar caché de compilación
        uses: actions/cache/save@v4
        with:
          path: target
          key: target-cache-${{ runner.os }}-${{ hashFiles('**/pom.xml', '**/*.java') }}

  # --------------------- DESPLEGAR EN SERVIDOR DE DESARROLLO ---------------------
  arrancar_en_servidor_de_desarrollo:
    needs: compilar_y_testear
    runs-on: self-hosted
    steps:
      # Descargar el código (por si es necesario para el docker-compose)
      - uses: actions/checkout@v4

      # Desplegar con docker compose (usa el nombre del proyecto y los puertos desde secretos)
      - name: Desplegar contenedor de desarrollo
        env:
          COMPOSE_PROJECT_NAME: buenaspracticas-dev
          PUERTO_DESARROLLO: ${{ secrets.PUERTO_DESARROLLO }}
          PUERTO_DB_DESARROLLO: ${{ secrets.PUERTO_DB_DESARROLLO }}
        run: docker compose -f compose-desarrollo.yaml up -d
